<%= form_for(@resume_entry) do |f| %>
  <% if @resume_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resume_entry.errors.count, "error") %> prohibited this resume_entry from being saved:</h2>

      <ul>
      <% @resume_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
